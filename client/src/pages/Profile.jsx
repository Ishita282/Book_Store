import React from "react";

const Profile = () => {
  return (
    <div>
      <h2>Profile</h2>
      <p>Nmae: {user.name}</p>
      <p>Email: {user.email}</p>
    </div>
  );
};

export default Profile;
